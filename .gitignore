# dependencies
node_modules/
.pnp
.pnp.js

# builds
dist/
build/
.next/
out/
*.tsbuildinfo

# turbo
.turbo/

# env — never commit secrets (PRD E-2)
.env
.env.local
.env.*.local
!.env.example
!.env.development.example

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/

# test output
coverage/
.nyc_output/

# editors / os
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# generated contract artefacts are committed intentionally:
# backend/openapi.json is tracked so CI can diff the contract (PRD B-4, E-16)

# never store uploaded media in the repo — STORAGE_ROOT lives outside it
storage/
uploads/
